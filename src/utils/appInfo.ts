/** Displayed on the Settings > About screen. Bump this alongside meaningful
 * releases (kept independent of package.json so it can be shown without a
 * build-time env plumbing step). */
export const APP_VERSION = '1.0.0'
